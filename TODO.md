# TODO / Backlog

## 🚧 待开发

### Audio 模式下载失败时回退为视频下载
**优先级**: P2 | **日期**: 2026-06-14 | **ID**: 2

当 Audio 模式被触发时，如果无法下载纯音频，当前行为是下载低质量视频。应改为：纯音频下载失败时，自动回退到正常视频下载，而非降质量视频。

---

### 任务完成后支持切换视频模式并下载高清版
**优先级**: P2 | **日期**: 2026-06-14 | **ID**: 3

音频模式任务完成后，允许用户手动切换到视频模式，触发高清视频的补充下载。满足用户先快速获取音频内容、后续补充高清视频的使用场景。

---

### 添加视频截图功能并关联时间点
**优先级**: P2 | **日期**: 2026-06-21

学习视频时支持手动截图，截图自动绑定当前播放时间点。设计哲学：视频同时包含视觉信息（图表、代码、公式等）和音频信息，仅依赖音频转录会丢失画面内容；截图+时间戳将两者关联，保留完整学习上下文。

---

### 字幕翻译质量 eval 测试套件
**优先级**: P3 | **日期**: 2026-06-21

为 translate 步骤建立翻译质量评估测试套件。使用 mock `llm_engine.sh` 替换真实 LLM 调用，对覆盖完整三阶段流程（分块 → LLM 调用 → 组装）的端到端集成路径进行回归测试。包含：典型讲座英文字幕的参考翻译对照、`zh_prev_tail` 上下文接续效果验证、部分 chunk 失败时 `original_zh.md` 降级输出的正确性。目标：任意代码改动后可一键验证翻译流程不退化，而无需真实 LLM 调用。

---

### 支持待处理视频队列与定时批量触发
**优先级**: P2 | **日期**: 2026-06-21

允许用户将想处理的视频 URL 提前加入待处理队列，在空闲时段由外部 Agent 定时任务自动触发批量处理，无需手动逐条提交。满足"收集内容 → 集中处理"的异步使用场景。

---

### 美化 Web 模块 Markdown 渲染并支持多风格模板
**优先级**: P2 | **日期**: 2026-06-27

对 Web 模块现有 Markdown 渲染效果进行深化：先打磨出一套美观的默认展示模板，再将样式配置抽象为可切换的多风格体系（如学术风、极简风等），为后续跨产品复用做铺垫。

---

### 为 Web 模块新增 Markdown 浏览与笔记功能
**优先级**: P2 | **日期**: 2026-06-27

横向扩展 Web 模块的内容类型：在现有音频、文本之外，新增 Markdown 文件浏览和笔记记录两个模块。核心理念：学习与记笔记是统一过程，应在同一入口整合所有学习资源（视频、音频、文章、笔记），而非分散各处。两个模块设计为独立组件，可抽出在衍生产品中复用。

---

## ✅ 已完成

- 按 DAG 拓扑支持无依赖任务并行执行（2026-06-23，`runTask` 池式并发，默认 N=3）
- 并行化 translate 节点以降低长视频翻译耗时（2026-06-26，整体式分页并行翻译：200 行/页、5 路并发、页间缝合、格式校验；同时新增 AI 字幕预合并提升可读性）
- 探索翻译后中文字幕按原时间戳重新分段的方案（2026-06-26，由 AI 字幕预合并 + 整体式翻译取代：LLM 直接将中文分配到句子级时间戳，无需事后回填）
- 研究单 session 串联生成 article 与 summary（2026-06-26）
- 修复详情页总结/文章模块大屏左对齐问题（2026-06-26）
